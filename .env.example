# Copy to .env and fill in values. Never commit .env to git.

FIRECRAWL_API_KEY=
GEMINI_API_KEY=
SLACK_WEBHOOK_URL=

# Optional
OUTPOST_THREAT_THRESHOLD=7

# Supabase (use service_role only in server/CI — never in a browser)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=
